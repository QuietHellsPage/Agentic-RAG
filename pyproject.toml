[tool.mypy]
python_version = "3.13"

[[tool.mypy.overrides]]
module = "src.embeddings.embedder"
disable_error_code = [
    "override",
    "return-value",
]

[tool.pylint.MASTER]
max-locals = 20

[tool.pylint.'design']
min-public-methods = 1
